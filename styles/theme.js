import {
    createTheme
} from "@mui/material";

const theme = createTheme({
    palette: {
        type: "dark",
        primary: {
            main: "#990000"
        },
        secondary: {
            main: "#1f1f1f"
        },
        background: {
            main: "#1f1f1f"
        }
    }
});

export default theme;